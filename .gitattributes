# Every SVG in assets/ is regenerated by scripts/build.mjs: on Windows locally and
# on ubuntu-latest in the signals workflow. Pinning LF stops those two from
# producing diffs that are nothing but line endings, which would make the daily
# refresh commit noise instead of signal.
* text=auto eol=lf
*.png binary
*.svg text eol=lf
